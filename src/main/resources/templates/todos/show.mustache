{{>layouts/header}}

    <table class="table">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Title</th>
            <th scope="col">Content</th>
        </tr>
        </thead>
        <tbody>
        {{#todo}}
        <tr>
            <th scope="row">{{id}}</th>
            <td>{{title}}</td>
            <td>{{content}}</td>
        </tr>
        {{/todo}}
        </tbody>
    </table>
<a href="/todos/{{todo.id}}/edit" class="btn btn-primary">Edit</a>
<a href="/todos/{{todo.id}}/delete" class="btn btn-danger">Delete</a>
<a href="/todos" class="btn btn-primary">Back to List</a>
{{>layouts/footer}}