{{>layouts/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>
    </tr>
    </thead>
    <tbody>
    {{#todoList}}
        <tr>
            <th scope="row">{{id}}</th>
            <td><a href="/todos/{{id}}">{{title}}</a></td>
            <td>{{content}}</td>
        </tr>
    {{/todoList}}
    </tbody>
</table>
<a href="/todos/newTodo">New Todo</a>

{{>layouts/footer}}